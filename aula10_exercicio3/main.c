#include <stdio.h>
#include <stdlib.h>

int main()
{
    int contador = 100;

    while (contador <= 110)
    {
        printf("o valor do contador é %d \n",contador);
        contador = contador +1;
    }
}
