#include <stdio.h>
#include <stdlib.h>

int main()
{
     // inicio do algoritmo, começa a contar do valor zero
    int contador = 0; //inicio do contador
    while (contador <= 20)//Limite do contador
    {
    //Meio do algoritmo
    //processo de contagem da variavel
        printf("%d\n", contador);
        contador = contador + 1;
    }
}
