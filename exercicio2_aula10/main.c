#include <stdio.h>
#include <stdlib.h>

int main()
{
    int contador = 10;

    while (contador >= 1)
    {

        printf("o valor do contador é %d \n",contador);
        contador = contador -1;
    }
}
